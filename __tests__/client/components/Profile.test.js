// Should take in a user object
// Should take in an uploaded videos list
// .profile-username should contain `username`
// .profile-videos children should contain the length the list passed in
// [0] should have a title of `title1`, description of `description1`

test('shoud pass', () => {
  expect(true).toEqual(true)
})
